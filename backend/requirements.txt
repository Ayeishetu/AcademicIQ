fastapi==0.115.12
uvicorn[standard]==0.34.3
python-multipart==0.0.20
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
sqlalchemy>=2.0.41
greenlet>=3.1.0
aiosqlite==0.21.0
pydantic>=2.11.4
pydantic-settings>=2.9.1
email-validator==2.3.0

# RAG / AI
tiktoken>=0.9.0
# sentence-transformers intentionally removed: it pulls in PyTorch (~300 MB),
# which blows Render's 512 MB free-tier RAM limit.
# Embeddings now use chromadb's built-in ONNX function (onnxruntime, ~80 MB).
anthropic==0.49.0

# Vector DB — includes onnxruntime + MiniLM-L6-v2 ONNX model for embeddings
chromadb>=0.6.3

# Document parsing
PyMuPDF==1.25.5
python-docx==1.1.2

# Utilities
python-dotenv==1.1.0
httpx==0.28.1
